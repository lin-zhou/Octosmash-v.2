class Player {
    constructor(number, character) {
        this.number = number;
        this.character = character;
    }

    constructor(number, character, name) {
        constructor(number, character);
        this.name = name;
    }

    // Default start location based on number and character modifiers
}